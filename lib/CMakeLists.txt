# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-display
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX
include(GrMiscUtils)
include(GrPython)


########################################################################
# Set QT generation stuff
########################################################################
set(display_moc_hdrs
    showpngpicture.h
    show_text_window.h
)

QT5_WRAP_CPP(display_moc_srcs ${display_moc_hdrs})
QT5_WRAP_UI(display_ui_hdrs showpngpicture.ui)

list(APPEND display_sources
    ${display_moc_srcs}
    ${display_ui_hdrs}
    show_image_impl.cc
    showpngpicture.cc
    show_text_impl.cc
    show_text_window.cc
    display_text_msg_impl.cc
)

set(display_sources "${display_sources}" PARENT_SCOPE)
if(NOT display_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT display_sources)

add_library(gnuradio-display SHARED ${display_sources})
target_link_libraries(gnuradio-display PUBLIC
	gnuradio::gnuradio-runtime
        gnuradio::gnuradio-blocks
        Qt5::Widgets
        Python::Python)
target_include_directories(gnuradio-display
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
	PRIVATE
	${CMAKE_CURRENT_BINARY_DIR}
  )
set_target_properties(gnuradio-display PROPERTIES DEFINE_SYMBOL "gnuradio_display_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-display PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-display)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_display_sources
qa_show_text.cc
qa_show_image.cc
qa_display_text_msg.cc
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-display)

if(NOT test_display_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_display_sources)

foreach(qa_file ${test_display_sources})
    GR_ADD_CPP_TEST("display_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
